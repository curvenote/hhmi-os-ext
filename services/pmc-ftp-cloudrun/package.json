{
  "name": "@curvenote/pmc-ftp-service-cloudrun",
  "description": "Bundled PMC FTP service",
  "version": "0.0.1",
  "private": true,
  "license": "",
  "author": "",
  "engines": {
    "node": "^22.0.0"
  },
  "scripts": {
    "start": "node index.js",
    "build:service": "cd ../../packages/pmc-ftp-service && bun run build && cd ../.. && cp packages/pmc-ftp-service/dist/* services/pmc-ftp-cloudrun && cd services/pmc-ftp-cloudrun",
    "build": "bun run build:service && ./build.sh",
    "deploy": "./deploy.sh",
    "build:local": "bun run build:service && docker build --tag pmc-ftp .",
    "dev": "bun run build:local && ./run.sh"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/ssh2-sftp-client": "9.0.4",
    "ssh2-sftp-client": "^12.0.0"
  }
}
