---
'@hhmi/pmc': patch
---

Add missing transitions from `REVIEWER_APPROVED_INITIAL` to `REQUEST_NEW_VERSION` and `NO_ACTION_NEEDED`, update the PMC workflow Mermaid diagram, and replace the split button with a kebab menu for workflow actions.
