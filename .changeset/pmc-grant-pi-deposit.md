---
"@hhmi/pmc": minor
---

Enrich HHMI grant deposits with awardee PI (`fname`, `lname`, `email`) looked up from synced grant records, surface grant ID and email in the grant picker, and bump `pmc-utils` to 0.4.0 so FTP renders nested `<PI>` in `bulk_meta.xml`.
