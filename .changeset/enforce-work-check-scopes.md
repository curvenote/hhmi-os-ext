---
'@hhmi/checks-proofig': patch
'@hhmi/checks-text-integrity': patch
---

Enforce work check scopes on extension actions and UI: require `work:checks:read` to view checks and download stored PDFs, and `work:checks:dispatch` to run, retry, or call third-party check services.
