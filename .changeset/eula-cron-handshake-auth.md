---
'@hhmi/checks-text-integrity': patch
---

Use endpoint-scoped handshake auth for the EULA cache refresh cron webhook, remove `eulaCronSecret`, add builtin cron install in admin, and document setup in `docs/eula-cron.md`.
