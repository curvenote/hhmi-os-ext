import { FileText } from 'lucide-react';

export function TextIntegrityIcon({ className }: { className?: string }) {
  return <FileText className={className} />;
}

export function TextIntegrityLogo({ className }: { className?: string }) {
  return <FileText className={className} />;
}

export function TextIntegrityLogoMono({ className }: { className?: string }) {
  return <FileText className={className} />;
}
