/** Shown on the upload step when DOCX→PDF conversion finished before Proofig submit. */
export function DocxPreparationCompleteNote() {
  return (
    <p className="text-sm text-muted-foreground">
      Your Word document was converted to PDF and is ready for upload.
    </p>
  );
}
