# Image Integrity Checks Extension

This extension provides image integrity checking functionality for Curvenote SCMS applications. It allows users to run integrity checks on images within Works.

## Features

- Image integrity checking task
- Custom icon for the extension

## Configuration

The extension can be configured via the application configuration file. See `extension.schema.yml` for available configuration options.


